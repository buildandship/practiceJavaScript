FN, LN, DoB

radio btn -> Gender
drop down
1. All disabled fields, once first field vallidated then second field get enabled
-- Validate each field
If all fields get validated then submit get enabled;
Create a form
Add fields
5 fields
-------------------------------------------------
Tasks1:
There are 5 fields all are disabled except first field. All fields have some validation. 
If first field is validated the enable next field and then next and so on. After that submit the page and get the message.

5 fields are
<fieldset>	disabled
<input>	    disabled
<optgroup>	disabled
<option>	disabled
<select>	disabled
<textarea>  disabled
<button>	disabled

